# ============================================================
# Smart Control Security - Backend (Python 3.11+)
# Dependencias de producción
# ============================================================
# Las versiones son mínimos compatibles; pip resolverá la más
# reciente compatible al instalar.

# --- Framework web ---
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# --- Validación / Configuración ---
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- Base de datos ---
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.13.3
geoalchemy2>=0.15.2

# --- Seguridad y Auth ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=43.0.0

# --- Servicios externos AWS ---
boto3>=1.35.0

# --- Background jobs + cache ---
arq>=0.26.1
redis>=5.1.0

# --- Email ---
sendgrid>=6.11.0

# --- Push notifications ---
firebase-admin>=6.5.0

# --- Analytics (Fase 4) ---
pandas>=2.2.3
openpyxl>=3.1.5

# --- Observabilidad ---
structlog>=24.4.0
sentry-sdk[fastapi]>=2.16.0

# --- HTTP client (para llamadas a APIs externas) ---
httpx>=0.27.2
